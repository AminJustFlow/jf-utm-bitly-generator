{
  "name": "jf-link-generator-bot-node",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Node.js port of the JF Link Generator Bot for ClickUp Chat.",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node ./bin/server.js",
    "dev": "node --watch --watch-path=./bin --watch-path=./src --watch-path=./config --watch-path=./.env ./bin/server.js",
    "migrate": "node ./bin/migrate.js",
    "test": "node ./tests/run.js",
    "debug:webhook": "node ./scripts/debug-webhook.js"
  }
}
